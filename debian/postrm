#!/bin/sh
set -e
if [ "$1" = "purge" ] || [ "$1" = "remove" ]; then
    systemctl disable --now xray-vpn-refresh.timer 2>/dev/null || true
fi
if [ "$1" = "purge" ]; then
    rm -rf /etc/xray-vpn /run/xray-vpn /var/cache/xray-vpn
fi
systemctl daemon-reload || true
