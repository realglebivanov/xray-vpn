services:
  xray-vpn:
    build: .
    network_mode: host
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    volumes:
      - ./state.json:/etc/xray-vpn/state.json
      - cache:/var/cache/xray-vpn
    restart: unless-stopped

volumes:
  cache:
